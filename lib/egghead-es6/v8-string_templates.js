'use strict';

var salutation = 'Hello';
// Notice that it's not normal quotes, it's the other kind
var greeting = salutation + ', World!';

// this also respects white space across multiple lines
console.log(greeting);

var x = 1;
var y = 2;
var equation = x + ' + ' + y + ' = ' + (x + y);

// You can also evaluate stuff between the curly braces
console.log(equation);